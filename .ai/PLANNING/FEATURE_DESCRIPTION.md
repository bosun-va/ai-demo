# Update the TodoWidget

TODOs will now include:

- owner: string
- dueDate: Date
- priority: Low|Medium|High

Update the UI to display and up``date the new data.

Update the data handling code to store and load the new data.

When you are done with the changes, run the project to see the changes.  You might need to `npm install` first.
